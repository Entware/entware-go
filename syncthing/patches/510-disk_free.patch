--- a/src/github.com/syncthing/syncthing/lib/osutil/osutil.go
+++ b/src/github.com/syncthing/syncthing/lib/osutil/osutil.go
@@ -17,7 +17,6 @@
 	"strings"
 	"syscall"
 
-	"github.com/calmh/du"
 	"github.com/syncthing/syncthing/lib/sync"
 )
 
@@ -286,11 +285,11 @@
 }
 
 func DiskFreeBytes(path string) (free int64, err error) {
-	u, err := du.Get(path)
-	return u.FreeBytes, err
+	err = nil
+	return 4096*1024*1024, err
 }
 
 func DiskFreePercentage(path string) (freePct float64, err error) {
-	u, err := du.Get(path)
-	return (float64(u.FreeBytes) / float64(u.TotalBytes)) * 100, err
+	err = nil
+	return 100, err
 }
